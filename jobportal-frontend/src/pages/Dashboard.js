import React from "react";

function Dashboard() {
  // defines a functional react component named dashboard
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Welcome to your dashboard!</p>
    </div>
  );
}

// exports the dashboard component so that it can be used in other parts of the applications
export default Dashboard;
