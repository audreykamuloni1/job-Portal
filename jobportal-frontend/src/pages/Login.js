import React from "react";

function Login() {
  // defines a functional react component named Login
  return (
    <div>
      <h1>Login Page</h1>
      <p>Login form will go here.</p>
    </div>
  );
}

// exports the login component so that it can be used in other parts of the applications
export default Login;
