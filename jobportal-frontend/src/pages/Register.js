import React from "react";

function Register() {
  // defines a functional react component named register
  return (
    <div>
      <h1>Register Page</h1>
      <p>Registration form will go here.</p>
    </div>
  );
}

// exports the register component so that it can be used in other parts of the applications
export default Register;
